{% extends 'base.html.twig' %}

{% block title %}Edit Page{% endblock %}

{% block body %}
<div class="container">
    <h1>Edit Page</h1>
    <a class="btn btn-success" href="{{ path('app_page_index') }}">back to list</a>
    {{ include('page/_form.html.twig', {'button_label': 'Update'}) }}
</div>
{% endblock %}
